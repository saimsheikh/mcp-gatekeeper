# docs

Assets for the project README.

- `demo.gif` — the approval loop: a write call parks, a human approves in the
  web UI, the call completes. Regenerate with the steps in
  [`recording-the-demo.md`](recording-the-demo.md).
- `demo.mp4` — same recording as video, for platforms that handle GIFs badly.
